name: CI

on:
  push:
    branches: [main, master]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.11", "3.12", "3.13"]

    steps:
      - uses: actions/checkout@v5

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install package
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[dev,torch]"

      - name: Ruff lint
        run: ruff check src tests examples

      - name: Ruff format
        run: ruff format --check src tests examples

      - name: Type check
        run: |
          mypy --version
          python -m mypy --no-site-packages --explicit-package-bases -p greenai

      - name: Tests
        if: success() || failure()
        run: pytest

      - name: Build package
        if: success() || failure()
        run: |
          pip install build
          python -m build
